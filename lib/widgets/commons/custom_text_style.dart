import 'package:flutter/material.dart';

import '../../utils/app_colors.dart';

const titleStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 32, letterSpacing: 0.9);

const buttonTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 20, letterSpacing: 0.5);

const googlebuttonTextStyle =
    TextStyle(fontWeight: FontWeight.bold, fontSize: 16, letterSpacing: 0.5);

const underlineStyle = TextStyle(
    fontSize: 15,
    fontWeight: FontWeight.w700,
    decoration: TextDecoration.underline,
    color: AppColors.buttonColor);
